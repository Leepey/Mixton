// src/features/dashboard/hooks/useQueueManagement.ts
// Заглушка для будущего функционала
export const useQueueManagement = () => {
  return {
    queue: [],
    isProcessing: false,
    processQueue: () => {},
    queueStats: { total: 0, pending: 0, completed: 0 },
  };
};